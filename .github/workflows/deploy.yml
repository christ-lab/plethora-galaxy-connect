name: Build and Deploy

on:
  push:
    branches:
      - master # Use "main" if your branch is called main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Build site
        run: npm run build

      - name: Deploy to Hosting via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: 'ftp'
          local-dir: ./dist/ # Change to ./build/ if your build output is in "build"
          server-dir: /home/plethora/domains/plethoragroupofcompanies.com/
